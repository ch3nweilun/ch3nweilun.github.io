<div class="network-bg" aria-hidden="true">
  <span class="node node-a"></span>
  <span class="node node-b"></span>
  <span class="node node-c"></span>
  <span class="node node-d"></span>
  <span class="link link-a"></span>
  <span class="link link-b"></span>
  <span class="link link-c"></span>
</div>

<style>
  .network-bg {
    position: fixed;
    inset: 0;
    z-index: -2;
    overflow: hidden;
    background:
      radial-gradient(circle at 16% 18%, rgba(56, 189, 248, 0.28), transparent 28rem),
      radial-gradient(circle at 82% 10%, rgba(147, 197, 253, 0.3), transparent 25rem),
      radial-gradient(circle at 50% 90%, rgba(37, 99, 235, 0.12), transparent 30rem),
      linear-gradient(135deg, #ffffff 0%, #eef7ff 48%, #f8fbff 100%);
  }

  .network-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(37, 99, 235, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(37, 99, 235, 0.08) 1px, transparent 1px);
    background-size: 72px 72px;
    mask-image: radial-gradient(circle at center, black, transparent 76%);
  }

  .network-bg::after {
    content: '';
    position: absolute;
    inset: -20%;
    background: conic-gradient(from 180deg, transparent, rgba(56, 189, 248, 0.14), transparent, rgba(37, 99, 235, 0.08), transparent);
    animation: drift 26s linear infinite;
    opacity: 0.58;
  }

  .node,
  .link {
    position: absolute;
    display: block;
  }

  .node {
    width: 14px;
    height: 14px;
    border-radius: 999px;
    background: #2563eb;
    box-shadow: 0 0 22px rgba(56, 189, 248, 0.75), 0 0 52px rgba(37, 99, 235, 0.28);
    animation: pulse 4s ease-in-out infinite;
  }

  .node-a { left: 12%; top: 34%; }
  .node-b { left: 38%; top: 17%; animation-delay: 0.8s; }
  .node-c { right: 18%; top: 38%; animation-delay: 1.5s; }
  .node-d { left: 62%; bottom: 18%; animation-delay: 2.2s; }

  .link {
    height: 1px;
    transform-origin: left center;
    background: linear-gradient(90deg, transparent, rgba(37, 99, 235, 0.34), transparent);
    box-shadow: 0 0 18px rgba(56, 189, 248, 0.26);
  }

  .link-a { left: 13%; top: 35%; width: 31%; transform: rotate(-25deg); }
  .link-b { left: 39%; top: 19%; width: 44%; transform: rotate(18deg); }
  .link-c { left: 57%; bottom: 23%; width: 27%; transform: rotate(-34deg); }

  @keyframes drift {
    to { transform: rotate(360deg); }
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 0.72; }
    50% { transform: scale(1.45); opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .network-bg::after,
    .node { animation: none; }
  }
</style>
